import { createColumn, createTable, createSolidTable, ColumnDef } from '@tanstack/solid-table';
import { Component } from 'solid-js';

const SolidTable: Component = () => {
    return (
        <div>
            <h2>SolidTable</h2>
        </div>
    )
}

export default SolidTable;